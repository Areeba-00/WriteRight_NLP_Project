fastapi==0.115.0
uvicorn==0.30.6
pyspellchecker==0.8.2
pydantic==2.9.2

# Optional - only needed for Stage 2 of Network NER (fine-tuned spaCy model).
# The rule-based extractor works without these. Uncomment + reinstall to train:
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
spacy==3.7.5

# News NLP module (summarization + headline generation via HuggingFace).
# Models lazy-load on first request and cache in ~/.cache/huggingface.
transformers>=4.40.0
torch>=2.0.0
# Required by Michau/t5-base-en-generate-headline (T5Tokenizer is SentencePiece-based).
sentencepiece>=0.2.0
# Add to backend/requirements.txt
python-docx==1.1.0
pdfplumber==0.11.0
python-multipart==0.0.9
deep-translator==1.11.4
onnxruntime>=1.16.0
onnx>=1.14.0
onnxscript>=0.1.0


